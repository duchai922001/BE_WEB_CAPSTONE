export enum PromotionDiscountType {
  MONEY = 'MONEY',
  PERCENT = 'PERCENT',
}

export enum PromotionStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  DEFAULT = 'default',
  EXPIRED = 'expired',
}
